<?xml version="1.1" encoding="UTF-8"?>
<!--
  XSD reference for api-cli XML configs. This documents the grammar for editor
  tooling and `api-cli docs schema`. The authoritative parser is xmlsource.go;
  this schema is a guide, not the enforcement point (api-cli validates configs
  by loading them).

  Element content may interleave text with the placeholders <value>, <if> and
  <for>, which compile to Go text/template source.
-->
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" elementFormDefault="qualified">

  <!-- ===== Placeholders (mixed content) ===== -->
  <xs:complexType name="value">
    <xs:attribute name="name" type="xs:string"/>
    <xs:attribute name="expr" type="xs:string"/>
    <xs:attribute name="default" type="xs:string"/>
    <xs:attribute name="as" type="xs:string"/>
  </xs:complexType>

  <xs:complexType name="ifType" mixed="true">
    <xs:group ref="placeholders" minOccurs="0" maxOccurs="unbounded"/>
    <xs:attribute name="test" type="xs:string" use="required"/>
    <xs:attribute name="eq" type="xs:string"/>
  </xs:complexType>

  <xs:complexType name="forType" mixed="true">
    <xs:group ref="placeholders" minOccurs="0" maxOccurs="unbounded"/>
    <xs:attribute name="each" type="xs:string" use="required"/>
  </xs:complexType>

  <xs:group name="placeholders">
    <xs:choice>
      <xs:element name="value" type="value"/>
      <xs:element name="if" type="ifType"/>
      <xs:element name="else"><xs:complexType/></xs:element>
      <xs:element name="for" type="forType"/>
    </xs:choice>
  </xs:group>

  <!-- A run-of-the-mill text+placeholder content model (url, header, var, ...). -->
  <xs:complexType name="content" mixed="true">
    <xs:group ref="placeholders" minOccurs="0" maxOccurs="unbounded"/>
  </xs:complexType>

  <!-- ===== Executables: <run> is a request, argv, or a shell string ===== -->
  <xs:complexType name="run" mixed="true">
    <xs:choice minOccurs="0" maxOccurs="unbounded">
      <xs:element name="request" type="request"/>
      <xs:element name="argv" type="content"/>
      <xs:group ref="placeholders"/>
    </xs:choice>
  </xs:complexType>

  <xs:complexType name="request">
    <xs:sequence>
      <xs:element name="url" type="content"/>
      <xs:element name="query" type="query" minOccurs="0"/>
      <xs:choice minOccurs="0" maxOccurs="unbounded">
        <xs:element name="header" type="header"/>
        <xs:element name="if" type="headerIf"/>
      </xs:choice>
      <xs:element name="body" type="content" minOccurs="0"/>
      <xs:element name="response" type="response" minOccurs="0"/>
    </xs:sequence>
    <xs:attribute name="method" type="xs:string"/>
    <!-- Name of a <transport>; "http" selects the built-in client, opting this
         request out of a default transport. Omit to use the registry's
         default, if any. -->
    <xs:attribute name="transport" type="xs:string"/>
    <!-- Comma-separated error statuses that are an answer rather than a
         failure ("404" or "404,410"). The body reaches the caller with exit
         code 0. It needs the built-in client. -->
    <xs:attribute name="allow-status" type="xs:string"/>
  </xs:complexType>

  <!-- ===== Transports: programs that perform requests ===== -->
  <xs:complexType name="transports">
    <xs:sequence>
      <xs:element name="transport" maxOccurs="unbounded">
        <xs:complexType>
          <xs:sequence>
            <!-- Must be a command (argv or shell), never a <request>. Sees the
                 rendered request at .request.{method,url,body,headers,
                 header_lines}; its stdout is the response body. -->
            <xs:element name="run" type="run"/>
            <xs:element name="cwd" type="content" minOccurs="0"/>
            <!-- Omit to send the request body on stdin; declare it (even
                 empty) to send something else. -->
            <xs:element name="stdin" type="content" minOccurs="0"/>
          </xs:sequence>
          <xs:attribute name="name" type="xs:string" use="required"/>
          <xs:attribute name="default" type="xs:boolean"/>
        </xs:complexType>
      </xs:element>
    </xs:sequence>
  </xs:complexType>

  <!-- Settings for the process-wide download queue. One queue serves the whole
       run, so these are set once. The concurrency, download-dir and no-tui
       flags override them per invocation. -->
  <xs:complexType name="downloads">
    <xs:attribute name="concurrency" type="xs:positiveInteger"/>
    <!-- 0 is meaningful: report a failure immediately rather than retrying. -->
    <xs:attribute name="retries" type="xs:nonNegativeInteger"/>
    <xs:attribute name="dir" type="xs:string"/>
  </xs:complexType>

  <!-- A hand-off to the download queue. Leaf-only and repeatable: it runs after
       the steps and stands in for the leaf's <run>. With over=, it repeats per
       record of that list, with the record's keys promoted and the record at
       .item. A <url> rendering several lines yields one download per line. -->
  <xs:complexType name="download">
    <xs:sequence>
      <xs:element name="url" type="content"/>
      <!-- A file path; a directory when it ends in "/", names an existing one,
           or serves several URLs. Omit for the download directory, named by the
           URL or the response's Content-Disposition. -->
      <xs:element name="to" type="content" minOccurs="0"/>
      <!-- Optional. The file is verified as it streams and deleted rather than
           renamed if it does not match. Render the body empty to skip the check
           for one record of an over= list. -->
      <xs:element name="hash" type="hash" minOccurs="0"/>
      <xs:choice minOccurs="0" maxOccurs="unbounded">
        <xs:element name="header" type="header"/>
        <!-- Folded into one Cookie header. -->
        <xs:element name="cookie" type="header"/>
        <xs:element name="if" type="downloadAuthIf"/>
      </xs:choice>
    </xs:sequence>
    <xs:attribute name="over" type="xs:string"/>
    <xs:attribute name="when" type="xs:string"/>
    <!-- A <transport> name, as on <request>: the program fetches the file and
         its stdout is streamed to disk. Empty takes the registry default;
         "http" opts back to the built-in client. -->
    <xs:attribute name="transport" type="xs:string"/>
  </xs:complexType>

  <xs:complexType name="hash" mixed="true">
    <xs:group ref="placeholders" minOccurs="0" maxOccurs="unbounded"/>
    <xs:attribute name="algo" type="hashAlgo"/>
  </xs:complexType>

  <xs:simpleType name="hashAlgo">
    <xs:restriction base="xs:string">
      <xs:enumeration value="md5"/>
      <xs:enumeration value="sha1"/>
      <xs:enumeration value="sha256"/>
      <xs:enumeration value="sha512"/>
    </xs:restriction>
  </xs:simpleType>

  <xs:complexType name="downloadAuthIf">
    <xs:choice maxOccurs="unbounded">
      <xs:element name="header" type="header"/>
      <xs:element name="cookie" type="header"/>
    </xs:choice>
    <xs:attribute name="test" type="xs:string" use="required"/>
  </xs:complexType>

  <xs:complexType name="header" mixed="true">
    <xs:group ref="placeholders" minOccurs="0" maxOccurs="unbounded"/>
    <xs:attribute name="name" type="xs:string" use="required"/>
  </xs:complexType>

  <xs:complexType name="headerIf">
    <xs:sequence>
      <xs:element name="header" type="header" maxOccurs="unbounded"/>
    </xs:sequence>
    <xs:attribute name="test" type="xs:string" use="required"/>
  </xs:complexType>

  <xs:complexType name="query">
    <xs:choice minOccurs="0" maxOccurs="unbounded">
      <xs:element name="param" type="header"/>
      <xs:element name="if" type="paramIf"/>
    </xs:choice>
    <xs:attribute name="from" type="xs:string"/>
  </xs:complexType>

  <xs:complexType name="paramIf">
    <xs:sequence>
      <xs:element name="param" type="header" maxOccurs="unbounded"/>
    </xs:sequence>
    <xs:attribute name="test" type="xs:string" use="required"/>
  </xs:complexType>

  <!-- jq= is the jq program, a template that renders against the leaf context
       (.arg/.flag/.var/.entry/.env). A bare dotted name is a context path to a
       string holding the program instead. -->
  <xs:complexType name="response">
    <xs:attribute name="jq" type="xs:string"/>
  </xs:complexType>

  <!-- ===== Output shape ===== -->
  <xs:complexType name="fields">
    <xs:sequence>
      <xs:element name="field" type="field" minOccurs="0" maxOccurs="unbounded"/>
    </xs:sequence>
    <xs:attribute name="over" type="xs:string"/>
    <xs:attribute name="footer" type="xs:string"/>
    <!-- Template predicate over the format context (.data, .tty, .arg, .flag,
         .result, ...). A leaf may declare several <fields>, and each one whose
         predicate holds renders, in document order. -->
    <xs:attribute name="when" type="xs:string"/>
  </xs:complexType>

  <xs:complexType name="field" mixed="true">
    <xs:attribute name="name" type="xs:string" use="required"/>
    <xs:attribute name="default" type="xs:string"/>
    <xs:attribute name="truncate" type="xs:integer"/>
    <xs:attribute name="firstline" type="xs:boolean"/>
    <xs:attribute name="priority" type="xs:integer"/>
    <xs:attribute name="show_in" type="xs:string"/>
    <xs:attribute name="expr" type="xs:string"/>
  </xs:complexType>

  <!-- ===== Legacy <format>/<view> presentation ===== -->
  <xs:complexType name="formats">
    <xs:sequence>
      <xs:element name="format" maxOccurs="unbounded">
        <xs:complexType>
          <xs:sequence>
            <xs:element name="view" type="view" maxOccurs="unbounded"/>
          </xs:sequence>
          <xs:attribute name="name" type="xs:string" use="required"/>
          <xs:attribute name="input" type="xs:string"/>
          <xs:attribute name="when" type="xs:string"/>
        </xs:complexType>
      </xs:element>
    </xs:sequence>
  </xs:complexType>

  <xs:complexType name="view" mixed="true">
    <xs:group ref="placeholders" minOccurs="0" maxOccurs="unbounded"/>
    <xs:attribute name="name" type="xs:string" use="required"/>
    <xs:attribute name="when" type="xs:string"/>
    <xs:attribute name="default" type="xs:boolean"/>
  </xs:complexType>

  <xs:complexType name="formatRef" mixed="true">
    <xs:sequence>
      <xs:element name="view" type="view" minOccurs="0" maxOccurs="unbounded"/>
    </xs:sequence>
    <xs:attribute name="ref" type="xs:string"/>
    <xs:attribute name="input" type="xs:string"/>
    <xs:attribute name="when" type="xs:string"/>
  </xs:complexType>

  <!-- ===== Args / flags / vars / steps ===== -->
  <xs:complexType name="arg">
    <xs:attribute name="name" type="xs:string" use="required"/>
    <xs:attribute name="type" type="xs:string"/>
    <xs:attribute name="required" type="xs:boolean"/>
    <xs:attribute name="variadic" type="xs:boolean"/>
    <!-- A Go regular expression every supplied value must match. Required on
         each arg of a runnable= node, where it is what keeps an argument and a
         subcommand name apart. -->
    <xs:attribute name="pattern" type="xs:string"/>
    <xs:attribute name="description" type="xs:string"/>
  </xs:complexType>

  <xs:complexType name="flag">
    <xs:attribute name="name" type="xs:string" use="required"/>
    <xs:attribute name="short" type="xs:string"/>
    <xs:attribute name="type" type="xs:string"/>
    <xs:attribute name="default" type="xs:string"/>
    <xs:attribute name="required" type="xs:boolean"/>
    <xs:attribute name="conflicts" type="xs:string"/>
    <xs:attribute name="description" type="xs:string"/>
  </xs:complexType>

  <xs:complexType name="vars">
    <xs:sequence>
      <xs:element name="var" type="content" maxOccurs="unbounded"/>
    </xs:sequence>
  </xs:complexType>

  <xs:complexType name="preconditions">
    <xs:sequence>
      <xs:element name="precondition" type="content" maxOccurs="unbounded"/>
    </xs:sequence>
  </xs:complexType>

  <xs:complexType name="steps">
    <xs:sequence>
      <xs:element name="step" maxOccurs="unbounded">
        <xs:complexType>
          <xs:sequence>
            <!-- A command or a request, same as a leaf's <run>. Omit it to
                 reuse the leaf's effective run with this step's <entry>. -->
            <xs:element name="run" type="run" minOccurs="0"/>
            <xs:element name="entry" type="entry" minOccurs="0"/>
            <xs:element name="cwd" type="content" minOccurs="0"/>
            <xs:element name="stdin" type="content" minOccurs="0"/>
          </xs:sequence>
          <xs:attribute name="name" type="xs:string" use="required"/>
          <xs:attribute name="when" type="xs:string"/>
        </xs:complexType>
      </xs:element>
    </xs:sequence>
  </xs:complexType>

  <!-- entry carries request shape (path + query). The loader also accepts
       arbitrary user-defined child elements for non-HTTP commands; those are
       not enumerated here. -->
  <xs:complexType name="entry">
    <xs:all>
      <xs:element name="path" type="content" minOccurs="0"/>
      <xs:element name="query" type="entryQuery" minOccurs="0"/>
    </xs:all>
  </xs:complexType>

  <xs:complexType name="entryQuery">
    <xs:sequence>
      <xs:element name="param" type="header" minOccurs="0" maxOccurs="unbounded"/>
    </xs:sequence>
  </xs:complexType>

  <!-- ===== Command tree (recursive) ===== -->
  <xs:complexType name="command">
    <xs:sequence>
      <xs:element name="arg" type="arg" minOccurs="0" maxOccurs="unbounded"/>
      <xs:element name="flag" type="flag" minOccurs="0" maxOccurs="unbounded"/>
      <xs:element name="vars" type="vars" minOccurs="0"/>
      <xs:element name="run" type="run" minOccurs="0"/>
      <xs:element name="cwd" type="content" minOccurs="0"/>
      <xs:element name="stdin" type="content" minOccurs="0"/>
      <xs:element name="confirm" type="content" minOccurs="0"/>
      <xs:element name="preconditions" type="preconditions" minOccurs="0"/>
      <xs:element name="steps" type="steps" minOccurs="0"/>
      <xs:element name="entry" type="entry" minOccurs="0"/>
      <xs:element name="fields" type="fields" minOccurs="0" maxOccurs="unbounded"/>
      <xs:element name="format" type="formatRef" minOccurs="0"/>
      <xs:element name="download" type="download" minOccurs="0" maxOccurs="unbounded"/>
      <xs:element name="command" type="command" minOccurs="0" maxOccurs="unbounded"/>
    </xs:sequence>
    <xs:attribute name="name" type="xs:string" use="required"/>
    <xs:attribute name="description" type="xs:string"/>
    <xs:attribute name="passthrough" type="xs:boolean"/>
    <!-- A node with subcommands that also runs. Every <arg> then needs a
         pattern= that matches no subcommand name. -->
    <xs:attribute name="runnable" type="xs:boolean"/>
    <xs:attribute name="confirm" type="xs:string"/>
  </xs:complexType>

  <!-- ===== Root ===== -->
  <xs:element name="config">
    <xs:complexType>
      <xs:sequence>
        <xs:element name="description" type="xs:string" minOccurs="0"/>
        <xs:element name="vars" type="vars" minOccurs="0"/>
        <xs:element name="transports" type="transports" minOccurs="0"/>
        <xs:element name="downloads" type="downloads" minOccurs="0"/>
        <xs:element name="run" type="run" minOccurs="0"/>
        <xs:element name="cwd" type="content" minOccurs="0"/>
        <xs:element name="stdin" type="content" minOccurs="0"/>
        <xs:element name="formats" type="formats" minOccurs="0"/>
        <xs:element name="command" type="command" minOccurs="0" maxOccurs="unbounded"/>
      </xs:sequence>
      <xs:attribute name="name" type="xs:string" use="required"/>
      <xs:attribute name="schema" type="xs:string"/>
    </xs:complexType>
  </xs:element>

</xs:schema>
